{
  "name": "project-to-reproduce-error-in-chai-as-promissed",
  "version": "1.0.0",
  "description": "example of bug in chai-as-promissed lib",
  "main": "",
  "scripts": {
    "postinstall": "webdriver-manager update",
    "test": "protractor ./protractor.config.js",
    "e2e:live": "protractor --elementExplorer",
    "webdriver:start": "webdriver-manager start"
  },
  "author": "Artur Górski",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.4.1",
    "chai-as-promised": "^5.2.0",
    "cucumber": "^0.9.2",
    "httpster": "^1.0.1",
    "protractor": "^3.0.0",
    "protractor-cucumber-framework": "^0.3.3"
  }
}
